import React from 'react';

const Header = () => {
  return (
    <h2
      style={{
        backgroundColor: "lightblue",
        textAlign: "center",
        padding: "1rem",
      }}
    >
      React Tabanlı Çalışmalar
    </h2>
  );
};

export default Header;
